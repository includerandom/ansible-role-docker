# Docker edition
docker_edition: ce #or ee

# Define and enable repo by this role
docker_repo_enable: true
docker_mirror_host: https://download.docker.com
docker_mirror_host_key: '{{ docker_mirror_host }}/linux/{{ (ansible_distribution == "Centos") | ternary("centos","debian") }}/gpg'
docker_mirror_host_yum: '{{ docker_mirror_host }}/linux/{{ ansible_distribution | lower }}/{{ ansible_distribution_major_version }}/$basearch/stable'

# For Prometheus metrics passed by Docker
docker_metrics_address: "0.0.0.0:9323"

# Used only for Debian/Ubuntu. Switch 'stable' to 'edge' if needed.
docker_apt_release_channel: stable
docker_apt_arch: amd64
docker_apt_repository: "deb [arch={{ docker_apt_arch }}] https://download.docker.com/linux/{{ ansible_distribution|lower }} {{ ansible_distribution_release }} {{ docker_apt_release_channel }}"
docker_apt_ignore_key_error: true

# A list of users who will be added to the docker group.
docker_users: []
#  root: /root/.docker

# Define variables for auth in private repo
docker_private_repo_auth_hash: SomeAuthHashOnTheRegistry
docker_private_repo_auth_hosts:
  - host: repo.inf.example.host
    hash: "{{ docker_private_repo_auth_hash }}"
  - host: repo2.inf.example.host
    hash: "{{ docker_private_repo_auth_hash }}"

# Define address of local binary repo Docker (without http)
docker_insecure_registry: "--insecure-registry {{ docker_private_repo_auth_host }}:9080 --insecure-registry {{ docker_private_repo_auth_host }}:9081 --insecure-registry {{ docker_private_repo_auth_host }}:9082"
docker_registry_mirror: "http://{{ docker_private_repo_auth_host }}:9080"

# Define partition with docker data
docker_data_root: /var/lib/docker
