{
        "auths": {
{% for regestry in docker_private_repo_auth_hosts  %}
                "{{ regestry.host }}": {
                        "auth": "{{ registry.hash }}"
                },
{% endfor %}
        }
}
